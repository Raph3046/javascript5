// else statement
function testElse(let) {
    let result = "";
    // Only change code belw this line

    if (let > 5) {
      result = "Bigger than 5";
    } else {
      result = "5 or Smaller";
    }

    // Only change the code above this line
    return result;
}

// Change this value to test
testElse(4);